# Production Environment Configuration for viament.alwood.dev
# Copy this to .env and fill in your actual values

# Grok API Key (REQUIRED)
GROK_API_KEY=sk-your-production-grok-key-here
GROK_MODEL=grok-4-fast

# Production Domain Configuration (for Cloudflare Tunnel)
NEXT_PUBLIC_API_URL=https://viament.alwood.dev/api
CORS_ORIGIN=https://viament.alwood.dev

# Notes:
# - NEXT_PUBLIC_API_URL tells frontend where to find the backend API
# - CORS_ORIGIN allows the backend to accept requests from your domain
# - Only port 3000 needs to be exposed through Cloudflare Tunnel
# - Backend (port 8000) communicates internally via Docker network
